# Anthropic API key for LLM-enhanced entity resolution, topic labeling, and summarization.
# Without this, the pipeline uses fuzzy matching + keyword extraction (still works, lower quality).
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-key-here
